-- generated by cds-compiler version 2.15.10
VIEW Service_Alarm AS SELECT
  Alarm_0.ID,
  Alarm_0.monitoredData_ID,
  Alarm_0.scenario_ID,
  Alarm_0.pacient_ID
FROM eespc_app_Alarm AS Alarm_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN Service_MonitoredData AS monitoredData ON (monitoredData.ID = monitoredData_ID),
  MANY TO ONE JOIN Service_AlarmScenarios AS scenario ON (scenario.ID = scenario_ID),
  MANY TO ONE JOIN Service_Pacient AS pacient ON (pacient.ID = pacient_ID)
)